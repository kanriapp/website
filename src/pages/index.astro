---
import Hero from "@/components/sections/Hero.astro"
import BaseLayout from "@/layouts/BaseLayout.astro"

import { motion } from "framer-motion"
---

<BaseLayout>
  <div class="relative">
    <Hero />
    <motion.div
      initial={{ opacity: 0 }}
      animate={{ opacity: 0.3 }}
      transition={{ duration: 0.6, ease: "easeOut", delay: 1.3 }}
      client:load
      className="hidden lg:block absolute top-1/3 left-[5%] w-11/12 h-[400px] bg-primary rounded-full filter blur-[72px] opacity-30"
    >
    </motion.div>
  </div>
  <h1 class="pt-20 bg-background z-10 block">Next section</h1>
</BaseLayout>


