---
import { motion } from "framer-motion"
import { Icon } from "astro-icon/components"
---

<section class="w-full mt-48 md:mt-20 lg:mt-0 py-12 md:pb-24 lg:pb-32 xl:pb-48 bg-card z-50">
  <div class="relative">
        <div class="container space-y-12 px-4 md:px-6 relative">
            <motion.div
                initial={{ opacity: 0, y: 20 }}
                animate={{ opacity: 1, y: 0 }}
                transition={{ duration: 0.5 }}
                client:load
                className="flex flex-col items-start md:items-center justify-center space-y-4 text-left md:text-center"
            >
                <div class="space-y-2 mb-8">
                    <div class="inline-block rounded-lg bg-muted px-3 py-1 text-sm">Key Features</div>
                    <h2 class="text-3xl font-bold tracking-tight sm:text-5xl">What's Under the Hood</h2>
                    <p class="max-w-[900px] text-muted-foreground md:text-xl/relaxed lg:text-base/relaxed xl:text-xl/relaxed">
                        Kanri is packed with powerful features to help you stay organized and productive. It is built with modern technologies such as Nuxt, Tauri and TypeScript, ensuring a smooth and efficient user experience.
                    </p>
                </div>
            </motion.div>
            <motion.div
                initial={{ opacity: 0 }}
                whileInView={{ opacity: 1 }}
                transition={{ duration: 0.5 }}
                viewport={{ once: true }}
                client:load
                className="mx-auto grid max-w-6xl items-start gap-8 sm:grid-cols-2 md:gap-12 lg:grid-cols-3"
            >
                <motion.div
                    initial={{ opacity: 0, x: -20 }}
                    animate={{ opacity: 1, x: 0 }}
                    transition={{ duration: 0.5, delay: 0.3 }}
                    client:load
                    className="grid gap-4"
                >
                    <div class="bg-card rounded-lg p-4 flex items-start gap-4">
                        <div class="bg-primary rounded-md p-3 flex items-center justify-center">
                            <Icon name="lucide:layout-dashboard" class="w-6 h-6 text-primary-foreground" />
                        </div>
                        <div>
                            <h3 class="text-lg font-bold">Kanban Board</h3>
                            <p class="text-sm text-muted-foreground">Organize your tasks efficiently using Kanban boards with unlimited customizable boards, columns and cards. Perfect for visual task management and boosting productivity.</p>
                        </div>
                    </div>
                </motion.div>
                <motion.div
                    initial={{ opacity: 0, x: -20 }}
                    whileInView={{ opacity: 1, x: 0 }}
                    transition={{ duration: 0.5, delay: 0.4 }}
                    viewport={{ once: true }}
                    client:load
                    className="grid gap-4"
                >
                    <div class="bg-card rounded-lg p-4 flex items-start gap-4">
                        <div class="bg-primary rounded-md p-3 flex items-center justify-center">
                            <Icon name="lucide:file-text" class="w-6 h-6 text-primary-foreground" />
                        </div>
                        <div>
                            <h3 class="text-lg font-bold">Rich Text Support</h3>
                            <p class="text-sm text-muted-foreground">
                                Add detailed descriptions and format your tasks with rich text support, using a format similar to Markdown and making each card more informative and easy to understand.
                            </p>
                        </div>
                    </div>
                </motion.div>
                <motion.div
                    initial={{ opacity: 0, x: -20 }}
                    whileInView={{ opacity: 1, x: 0 }}
                    transition={{ duration: 0.5, delay: 0.5 }}
                    viewport={{ once: true }}
                    client:load
                    className="grid gap-4"
                >
                    <div class="bg-card rounded-lg p-4 flex items-start gap-4">
                        <div class="bg-primary rounded-md p-3 flex items-center justify-center">
                            <Icon name="lucide:wifi-off" class="w-6 h-6 text-primary-foreground" />
                        </div>
                        <div>
                            <h3 class="text-lg font-bold">Offline Support</h3>
                            <p class="text-sm text-muted-foreground">Work seamlessly without an internet connection; all your data can be stored locally to ensure privacy and constant accessibility.</p>
                        </div>
                    </div>
                </motion.div>
                <motion.div
                    initial={{ opacity: 0, x: -20 }}
                    whileInView={{ opacity: 1, x: 0 }}
                    transition={{ duration: 0.5, delay: 0.6 }}
                    viewport={{ once: true }}
                    client:load
                    className="grid gap-4"
                >
                    <div class="bg-card rounded-lg p-4 flex items-start gap-4">
                        <div class="bg-primary rounded-md p-3 flex items-center justify-center">
                            <Icon name="lucide:palette" class="w-6 h-6 text-primary-foreground" />
                        </div>
                        <div>
                            <h3 class="text-lg font-bold">Customizable Themes</h3>
                            <p class="text-sm text-muted-foreground">
                                Personalize your workspace with customizable themes and backgrounds to create a visually appealing and comfortable environment. Dark mode, light mode or Catppuccin theme, we've got it all.
                            </p>
                        </div>
                    </div>
                </motion.div>
                <motion.div
                    initial={{ opacity: 0, x: -20 }}
                    whileInView={{ opacity: 1, x: 0 }}
                    transition={{ duration: 0.5, delay: 0.7 }}
                    viewport={{ once: true }}
                    client:load
                    className="grid gap-4"
                >
                    <div class="bg-card rounded-lg p-4 flex items-start gap-4">
                        <div class="bg-primary rounded-md p-3 flex items-center justify-center">
                            <Icon name="lucide:keyboard" class="w-6 h-6 text-primary-foreground" />
                        </div>
                        <div>
                            <h3 class="text-lg font-bold">Keyboard Shortcuts</h3>
                            <p class="text-sm text-muted-foreground">
                                Navigate your boards and cards quickly and efficiently with keyboard shortcuts designed for power users to maximize productivity.
                            </p>
                        </div>
                    </div>
                </motion.div>
                <motion.div
                    initial={{ opacity: 0, x: -20 }}
                    whileInView={{ opacity: 1, x: 0 }}
                    transition={{ duration: 0.5, delay: 0.8 }}
                    viewport={{ once: true }}
                    client:load
                    className="grid gap-4"
                >
                    <div class="bg-card rounded-lg p-4 flex items-start gap-4">
                        <div class="bg-primary rounded-md p-3 flex items-center justify-center">
                            <Icon name="lucide:download" class="w-6 h-6 text-primary-foreground" />
                        </div>
                        <div>
                            <h3 class="text-lg font-bold">Data Export</h3>
                            <p class="text-sm text-muted-foreground">
                                Easily back up or transfer your Kanban boards with data export options, ensuring your information is always secure and portable.
                            </p>
                        </div>
                    </div>
                </motion.div>
            </motion.div>
        </div>
        <motion.div
            initial={{ opacity: 0 }}
            whileInView={{ opacity: 0.6 }}
            transition={{ duration: 0.6, ease: "easeOut" }}
            viewport={{ once: true }}
            client:load
            className="hidden lg:block absolute top-24 left-[22%] w-[800px] h-[40px] bg-blue-500 rounded-full filter blur-3xl opacity-40"
        >
        </motion.div>
  </div>
</section>